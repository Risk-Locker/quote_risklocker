# Migration files stay LF on disk for deterministic diffs. Ledger checksums are
# computed from line-ending-normalized bytes (see app/db/migrations.py), so
# CRLF/LF checkout differences can no longer cause checksum drift.
migrations/*.sql text eol=lf
